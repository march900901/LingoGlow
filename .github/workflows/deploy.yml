name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Backup custom index.html and pubspec
        run: |
          cp pubspec.yaml pubspec.yaml.bak
          if [ -d web ] && [ -f web/index.html ]; then cp web/index.html index.html.bak; fi

      - name: Generate Web platform template
        run: flutter create --platforms=web .

      - name: Restore custom index.html and pubspec
        run: |
          cp pubspec.yaml.bak pubspec.yaml
          if [ -f index.html.bak ]; then cp index.html.bak web/index.html; fi

      - name: Install dependencies
        run: flutter pub get

      - name: Build Web Version with debugging log
        run: |
          flutter build web --release --base-href "/LingoGlow/" > build_output.log 2>&1 || (
            echo "=== BUILD FAILED ==="
            git config --global user.name "github-actions[bot]"
            git config --global user.email "github-actions[bot]@users.noreply.github.com"
            git checkout -b build-failures || git checkout build-failures
            git add build_output.log
            git commit -m "build failure logs"
            git push origin build-failures --force
            exit 1
          )

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build/web
